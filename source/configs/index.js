import { apiConfig, baseUrl } from './apiConfig';
import {
  COLORS,
  PALETTE,
  SCROLL_VIEW_CONTAINER,
  SNACKBAR_OPTIONS,
} from './styles';
import { HEIGHT, WIDTH, STATUS_BAR_HEIGHT, rh, rw } from './dimensions';
import { faLocale } from './calendarLocaleConfig';
import { months, days, getYears, dayNumbers } from './statics';
import { PUSHER_INSTANCE_ID } from './notificationConfigs';

export {
  apiConfig,
  baseUrl,
  COLORS,
  SCROLL_VIEW_CONTAINER,
  SNACKBAR_OPTIONS,
  HEIGHT,
  WIDTH,
  faLocale,
  months,
  days,
  getYears,
  dayNumbers,
  STATUS_BAR_HEIGHT,
  rh,
  rw,
  PALETTE,
  PUSHER_INSTANCE_ID,
};
